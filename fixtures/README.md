# Fixtures de motores

Screenshots ANONIMIZADOS para testear los motores (F1/F4/F5) con vitest. Se cargan en Fase 1+.

Reglas:
- Nada de datos personales reales: caras pixeladas o sintéticas, nombres/handles inventados, sin metadata EXIF.
- Un subdirectorio por motor: `audit/`, `chat/`, `profile-read/`.
- Los fixtures de contratos JSON (ejemplos §6) viven en `packages/contracts/fixtures/`.
